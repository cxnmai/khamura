use gpui::Hsla;

/// The base color used for the camera's letterbox bars.
pub const CAMERA_LETTERBOX_COLOR: Hsla = Hsla {
    h: 0.0,
    s: 0.0,
    l: 0.0,
    a: 1.0,
};

/// The opacity applied to the camera's letterbox bars.
pub const CAMERA_LETTERBOX_OPACITY: f32 = 0.7;
